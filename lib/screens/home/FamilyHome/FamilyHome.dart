import 'package:flutter/material.dart';

class FamilyHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Family Home'),
      ),
      body: Center(
        child: Text('Welcome to the Family Home Screen!'),
      ),
    );
  }
}